let age=18
let name="shivam"
let isLoggedIn=true
//bigint(use for when you have very big number then we use)
//string(use in dobule code)
//boolean(user isloggedin or not means true or false)
//null(stand alone value means empty state )
//undefined(variable declare kr diye pr value assign nhi kiye)
//symbol(use foe uniqueness)
console.log(typeof age);